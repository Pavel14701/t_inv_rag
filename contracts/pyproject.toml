[project]
name = "dte-contracts"
version = "0.1.0"
description = "Shared message contracts for white API <-> local GPU node (TZ-09, TZ-08)"
requires-python = ">=3.12"
dependencies = [
    "msgspec>=0.19",
]

[dependency-groups]
dev = [
    "pytest==9.1.1",
]

[tool.uv]
package = false
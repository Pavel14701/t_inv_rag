[project]
name = "dte-risk"
version = "0.1.0"
description = "Deterministic risk engine: config-driven rule pipeline (TZ-11)"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest==9.1.1",
    "pytest-mock>=3.15.1",
]

[tool.uv]
package = false
[project]
name = "dte-backtest"
version = "0.1.0"
description = "Deterministic backtest engine: execution, portfolio, metrics (TZ-04)"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "polars>=1.42.1",
]

[dependency-groups]
dev = [
    "pytest==9.1.1",
    "pytest-mock>=3.15.1",
]

[tool.uv]
package = false